name: Deploy Frontend to Azure App Container

on:
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Log in to Azure Container Registry
        uses: azure/docker-login@v1
        with:
          login-server: YOUR_ACR_LOGIN_SERVER.azurecr.io
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Build and push Docker image
        run: |
          docker build . -f ./frontend/Dockerfile -t YOUR_ACR_LOGIN_SERVER.azurecr.io/freelancer-toolkit-frontend:${{ github.sha }}
          docker push YOUR_ACR_LOGIN_SERVER.azurecr.io/freelancer-toolkit-frontend:${{ github.sha }}

      - name: Deploy to Azure Container App
        uses: azure/container-apps-deploy-action@v1
        with:
          azure-container-registry-login-server: YOUR_ACR_LOGIN_SERVER.azurecr.io
          azure-container-app-name: YOUR_CONTAINER_APP_NAME
          resource-group: YOUR_RESOURCE_GROUP
          container-image-name: YOUR_ACR_LOGIN_SERVER.azurecr.io/freelancer-toolkit-frontend:${{ github.sha }}
